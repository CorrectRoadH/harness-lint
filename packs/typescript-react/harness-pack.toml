[pack]
id = "typescript-react"
name = "TypeScript React"
version = "0.1.0"
description = "Rules for React codebases using TypeScript and predictable component lifecycles."
license = "MIT"

[compat]
harness = ">=0.1.0"
grit = ">=0.1.0"
languages = ["typescript", "javascript"]

interface:
  display_name: "harness-lint"
  short_description: "Install, migrate, and author harness-lint rules"
  default_prompt: "Use $harness-lint to install or migrate harness-lint in this repo, manage rule packs, and turn recurring feedback into checked rules."

policy:
  allow_implicit_invocation: true
